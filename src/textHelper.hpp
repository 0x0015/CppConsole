#include <iostream>
using namespace std;

extern bool beginsWith(string source, string substring);


extern bool stringContains(string source, string substring);


extern int countFreq(const string str, const string sub);
